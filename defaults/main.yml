---
# defaults file for fail2ban

update_pkg: True

## Custom configuration
fail2ban_config:
 - "logtarget = /var/log/fail2ban.log" 
 - "loglevel = INFO"

## SSH Jail
ban_time: 60
ssh_port: ssh
ignore_ip: 127.0.0.1 
max_retry: 3
log_path: ''
